import { IconesPipePipe } from './icones-pipe.pipe';

describe('IconesPipePipe', () => {
  it('create an instance', () => {
    const pipe = new IconesPipePipe();
    expect(pipe).toBeTruthy();
  });
});
