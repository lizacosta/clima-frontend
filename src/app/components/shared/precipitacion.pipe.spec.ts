import { PrecipitacionPipe } from './precipitacion.pipe';

describe('PrecipitacionPipe', () => {
  it('create an instance', () => {
    const pipe = new PrecipitacionPipe();
    expect(pipe).toBeTruthy();
  });
});
