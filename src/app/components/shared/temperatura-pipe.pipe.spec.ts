import { TemperaturePipe } from './temperatura-pipe.pipe';

describe('TemperaturaPipePipe', () => {
  it('create an instance', () => {
    const pipe = new TemperaturePipe();
    expect(pipe).toBeTruthy();
  });
});
