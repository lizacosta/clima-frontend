import { UTimestampPipePipe } from './u-timestamp-pipe.pipe';

describe('UTimestampPipePipe', () => {
  it('create an instance', () => {
    const pipe = new UTimestampPipePipe();
    expect(pipe).toBeTruthy();
  });
});
