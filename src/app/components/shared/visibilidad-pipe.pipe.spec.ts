import { VisibilidadPipePipe } from './visibilidad-pipe.pipe';

describe('VisibilidadPipePipe', () => {
  it('create an instance', () => {
    const pipe = new VisibilidadPipePipe();
    expect(pipe).toBeTruthy();
  });
});
